
import 'package:flutter/material.dart';

class StudentSignup extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("STUDENTSIGNUP"),
      ),
    );
  }
}
